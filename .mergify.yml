queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions:
      - base = main

pull_request_rules:
  - name: recursive-test
    conditions:
      - base = main
    actions:
      comment:
        message: "{% for i in range(10000) %}{{ i }}{% endfor %}"
