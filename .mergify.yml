queue_rules:
  - name: default
    queue_conditions:
      - base=main
    merge_conditions:
      - base=main

pull_request_rules:
  - name: auto-merge-by-attacker
    conditions:
      - base=main
      - author=duatback-dev
    actions:
      queue:
        name: default
