queue_rules:
  - name: default
    queue_conditions:
      - base = main
    merge_conditions:
      - base = main

pull_request_rules:
  - name: long-jinja
    conditions:
      - base = main
    actions:
      comment:
        message: "{{ 'a' * 1000000 }}"
