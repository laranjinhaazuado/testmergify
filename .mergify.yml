queue:
  - name: default
    conditions:
      - base=main
      - "#approved-reviews-by >= 1"
    actions:
      merge:
        method: merge

pull_request_rules:
  - name: auto-comment
    conditions:
      - base=main
    actions:
      comment:
        message: "PR #{{ number }} by {{ author }}"
